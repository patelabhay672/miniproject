<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>miniproject</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h1 class="text-3xl mb-5">Login</h1>

      <div>
        <a href="/" class="mt-3 mb-5">register</a>
        <a href="/logout" class="mt-3 mb-5">logout</a>
      </div>
      <form class="" action="/login" method="post">
        <input
          class="px-3 py2 outline-none bg-transparent border-2 text-white rounded"
          type="email"
          placeholder="email"
          name="email"
        />
        <input
          class="px-3 py2 outline-none bg-transparent border-2 text-white rounded"
          type="password"
          placeholder="password"
          name="password"
        />
        <input
          class="px-3 py-2 outline-none bg-blue-500 rounded"
          type="submit"
          value="login"
        />
      </form>
    </div>
  </body>
</html>
